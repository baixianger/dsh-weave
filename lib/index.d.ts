/** The application-layer protocol negotiated by dsh-mesh peers. */
export declare const DSH_MESH_ALPN: "dsh-mesh/1";

/** This package publishes the protocol contract before the transport runtime. */
export declare const DSH_MESH_STAGE: "design-preview";

/** The first public protocol revision. */
export declare const DSH_MESH_PROTOCOL_VERSION: 1;
